{
	"geode": "1.0.0",
	"version": "v5.0.0-alpha.8",
	"id": "hjfod.betteredit",
	"name": "BetterEdit",
	"developer": "HJfod",
	"description": "The ultimate editor improvement mod",
	"repository": "https://github.com/hjfod/BetterEdit",
	"settings": {
		"mouse-move-on-zoom": {
			"type": "bool",
			"default": true,
			"name": "Zoom Towards Cursor",
			"description": "When enabled, zooming in the editor with the scroll wheel will be zoomed toward the cursor and not the center of the screen. Not currently supported on MacOS"
		},
		"show-zoom-text": {
			"type": "bool",
			"default": true,
			"name": "Show Zoom Text",
			"description": "When enabled, flashes the current zoom level as text when you zoom in / out in the editor"
		},
		"show-percentage": {
			"type": "bool",
			"default": true,
			"name": "Show Percentage",
			"description": "When enabled, flashes the current percentage in the position slider when you move around the editor"
		},
		"enable-new-undo": {
			"type": "bool",
			"default": true,
			"name": "Better Undo",
			"description": "When enabled, allows undoing any action taken in the editor, like modifying object colors"
		},
		"selection-preview": {
			"type": "bool",
			"default": true,
			"name": "Selection Preview",
			"description": "Shows which objects are about to be selected when using Swipe"
		},
		"new-color-menu": {
			"type": "bool",
			"default": true,
			"name": "New Color Menu",
			"description": "Changes the object color selection menu to show more channels aswell as previews for colors and special colors"
		},
		"auto-show-grid": {
			"type": "bool",
			"default": true,
			"name": "Show Grid on Size Change",
			"description": "Automatically show the grid when its size is changed or it is locked"
		},
		"developer-mode": {
			"type": "bool",
			"default": false,
			"name": "Developer Mode",
			"description": "Enables some features intended for developers"
		}
	},
	"resources": {
		"files": [
			"scripts/*.swipe"
		],
		"spritesheets": {
			"UISheet": [
				"resources/*.png",
				"resources/history/*.png",
				"resources/objs/*.png"
			]
		}
	},
	"issues": {
		"info": "Please post your issues on the <cy>BetterEdit GitHub repository</c>.",
		"url": "https://github.com/hjfod/BetterEdit/issues/new"
	},
	"dependencies": [
		{
			"id": "hjfod.gmd-api",
			"version": "v1.0.0",
			"required": true
		},
		{
			"id": "hjfod.custom-keybinds",
			"version": "v1.0.0",
			"required": true
		},
		{
			"id": "geode.mouse-api",
			"version": "v1.0.0",
			"required": false
		}
	]
}
