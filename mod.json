{
	"geode": "2.0.0",
	"version": "v1.0.0",
	"gd": {
		"win": "2.204",
		"android64": "2.205"
	},
	"id": "be_team.betteredit",
	"name": "BetterEdit 2.2",
	"developer": "BE Team",
	"description": "The Ultimate Editor Mod",
	"repository": "https://github.com/HJfod/BetterEdit",
	"dependencies": [
        {
            "id": "geode.node-ids",
            "version": "v1.1.0",
            "importance": "required"
        }
    ],
	"settings": {
		"mouse-move-on-zoom": {
			"type": "bool",
			"default": true,
			"name": "Zoom Towards Cursor",
			"description": "When enabled and while holding down the CTRL key, zooming in the editor with the scroll wheel will be zoomed toward the cursor and not the center of the screen"
		},
		"better-font-select": {
			"type": "bool",
			"default": true,
			"name": "Better Font Select",
			"description": "Replaces the old font select screen with a better one with scrolling"
		},
		"scale-factor": {
			"type": "float",
			"default": 1.00,
			"max": 1.00,
			"min": 0.3,
			"name": "Scale Factor",
			"description": "The factor the UI should be scaled by",
			"control": {
                "slider": true
            }
		},
		"scale-pause": {
			"type": "bool",
			"default": true,
			"name": "Scale Pause?",
			"description": "Should it scale the pause menu"
		},
		"scale-build-tabs": {
			"type": "bool",
			"default": true,
			"name": "Scale Build Tabs",
			"description": "Should it scale the tabs above the object selection area?"
		}
	}
}
